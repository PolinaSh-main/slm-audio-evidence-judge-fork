# Минимальный набор для LLM-судьи (src/judge.py, src/judges/, src/run_eval.py) --
# без аудио/TTS/датасет-генерации из requirements.txt (нужны только инференс-пайплайну
# M2, судья их не импортирует -- см. grep по import в src/judge*.py/run_eval.py/metrics.py).
#
# torch ЯВНО пинован на 2.0.1 -- ту же версию, что и preinstalled в DataSphere в паре с
# torchvision/torchaudio/torchtext/torchdata (см. их конфликты в pip-логе от 27.07:
# "torchaudio 2.0.2+cu118 requires torch==2.0.1"). Изначально этот файл торч вообще не
# упоминал, рассчитывая, что pip не станет трогать уже стоящую версию -- но
# `pip uninstall torch` (разбор одной из поломок 27.07) снёс единственную копию
# полностью, и без явного пина accelerate's `torch>=2.0.0` тут же утягивает latest
# (2.13.0) заново. Явный пин защищает от обоих сценариев разом: если torch уже стоит
# 2.0.1 -- "Requirement already satisfied", ничего не качается; если стоит другая
# версия или отсутствует -- ставится ровно 2.0.1, а не latest.
torch==2.0.1
huggingface_hub>=0.36.2
transformers  # без искусственного пола >=5.14.1 из requirements.txt -- тот порог был нужен
              # только для Gemma-4/Qwen3.5/Qwen3.6 в лестнице кандидатов на Kaggle; здесь
              # только Qwen3-8B (см. src/judges/local_hf.py), она под преинсталленный
              # transformers заводится и без него -- пусть pip возьмёт то, что уже есть.
accelerate    # device_map="auto" в LocalHFJudge
bitsandbytes  # BitsAndBytesConfig (8-bit по умолчанию) в LocalHFJudge
rapidfuzz     # fuzzy-сверка категории A + кластеризация в scripts/entropy_baseline.py
